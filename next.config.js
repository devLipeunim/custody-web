/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // The floating dev badge sits over the page and would appear in a
  // screenshot or on the projector during the demo.
  devIndicators: false,
};
