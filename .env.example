# Custody Dashboard Environment Variables

# Backend API URL for Next.js Server Components
API_BASE=http://localhost:4000

# Backend API URL for Browser-side verification calls
# Note: For multi-device LAN testing, use the host machine's LAN IP address
NEXT_PUBLIC_API_BASE=http://localhost:4000
